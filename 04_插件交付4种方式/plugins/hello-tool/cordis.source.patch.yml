# 源码 patch，用于 `pnpm dsh web --patch plugins/hello-tool/cordis.source.patch.yml`。
# The relative entry is anchored to this file, so the package does not need to be
# installed into the selected profile.

- insert:
    - id: hello-tool
      name: './src/index.ts'
